components:
  parameters:
    ARef:
      type: string
      name: aref
      in: header
      description: The schema desc.
    BRef:
      in: path
      name: id
      required: true
      schema:
        $ref: 'reference.yaml#/components/schemas/EntityId'
