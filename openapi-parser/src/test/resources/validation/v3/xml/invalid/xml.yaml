openapi: 3.0.0
info:
  version: 1.0.0
  title: parameter
paths: {}
components:
  schemas:
    schema1:
      xml:
        name: name
        namespace: /my.url.com # invalid
        prefix: prefix
        attribute: true
        wrapped: false
